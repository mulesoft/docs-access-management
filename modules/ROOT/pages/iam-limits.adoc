= Limits in Access Management
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]

Access Management imposes the following limits on various features in an organization:

* Roles per client: 3000
* Clients per root organization: 10000
* Client management providers per root organization: 10
* Environments per organization: 150
* Sub organizations per root organization: 100
* Roles per organization: 500
* External access relations per organization: 20
* Teams per root organization: 1000
* Users per organization: 10000
* Roles per role group: 400
* Roles per user: 5000
* Role groups per user: 700
* Connected apps per organization: 200
* Scope assignments per connected app: 1000
* Authorized applications per user: 30
* Role assignments per team: 1000
* External group mappings per team: 100

--------- 
02/02/22

Access Management imposes the following limits on various features in an organization:

Organization limits:

* Sub organizations per root organization: 100
* Users per root or sub-organization: 10000
* Environments per root or sub organization: 150
* API Manager clients or runtime clients per root or sub organization: 10000 ->?? I don't really get what this is either.
* External access relations per root or sub organization: 20
* Permission roles per external organization: 1000 -> ??

Teams, roles, and user permissions:

* Teams per root organization: 1000
* Permission roles per team: 1000 -> Permissions per team?
* Rolegroups/deprecated roles per user: 700 -> Roles per user?
* Permission roles per api manager client or runtime client: 3000 -> ??
* Deprecated roles per root or sub organization: 500 -> ?? Roles each org can have configured?
* Permission roles per deprecated role: 400 -> Permissions each role can have associated with it? 
* Permission roles per user: 5000 ->?? individual permissions?
* External group mappings per team: 100 

Identity providers:

* Identity management provider keys per provider: 3
* Identity management providers per root or sub organization: 25

Client providers:

* Client management providers per root or sub organization: 10

Connected apps:

* Authorized applications per user: 30 -> Connected apps per user? or.
* External connected applications per root or sub organization: 20 -> Connected Apps accessible by external organizations.
* Internal connected applications per root or sub organization: 2000 -> Connected Apps within an organization?
* Scope assignments per connected application: 1000
